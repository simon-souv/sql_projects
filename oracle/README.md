# oracle scripts

| script name | description |
|-------------|-------------|
| drop_all_objects.sql | drop all objects in a schema provided at execution. Script to be executed with *SYS* user |
| drop_user_objects.sql | equivalent to `drop_all_objects.sql` script to execute directly with *SYS* user |
